export enum BetStatusEnum {
    APPROVED = 'approved',
    PENDING_APPROVAL = 'pending-approval',
    REJECTED = 'rejected',
    BY_ADMIN = 'by-admin',
}

export enum MatchTypeEnum {
    SCHEDULED = 'scheduled',
    LIVE = 'live',
    POPULAR = 'popular',
}